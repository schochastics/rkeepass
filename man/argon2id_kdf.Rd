% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read4.R
\name{argon2id_kdf}
\alias{argon2id_kdf}
\title{Derive a 32-byte key using Argon2id}
\usage{
argon2id_kdf(password, salt, time_cost, memory_kib, parallelism)
}
\arguments{
\item{password}{A character scalar: the user’s master password.}

\item{salt}{A raw vector containing the salt (typically 32 bytes from KdfParameters$S).}

\item{time_cost}{Integer: number of iterations (from KdfParameters$I).}

\item{memory_kib}{Integer: memory cost in KiB (from KdfParameters$M).}

\item{parallelism}{Integer: number of threads (from KdfParameters$P).}
}
\value{
A raw vector of 32 bytes (the KDF output).
}
\description{
This function wraps the native Rcpp implementation of the Argon2 key derivation
function (KDF) using the Argon2id variant. It is used internally to derive the
decryption key from a password in the KDBX4 file format.
}
\examples{
\dontrun{
key <- argon2id_kdf(
  password = "hunter2",
  salt = kdf_params$S,
  time_cost = kdf_params$I,
  memory_kib = kdf_params$M,
  parallelism = kdf_params$P
)
}
}
