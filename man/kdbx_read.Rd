% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kdbx_read.R
\name{kdbx_read}
\alias{kdbx_read}
\title{Read a KeePass Database File}
\usage{
kdbx_read(path, password = NULL, keyfile = NULL)
}
\arguments{
\item{path}{Character scalar. Path to the \code{.kdbx} file.}

\item{password}{Character scalar or \code{NULL}. The database master password.}

\item{keyfile}{Character scalar or \code{NULL}. Path to a key file.}
}
\value{
A data.frame with one row per entry and columns:
\describe{
\item{uuid}{Character. The unique identifier of the entry.}
\item{group_path}{Character. The slash-separated path of the group
containing this entry (e.g., \code{"Root/Internet"}).}
\item{title}{Character. The entry title.}
\item{username}{Character. The username field.}
\item{password}{Character. The password field (decrypted).}
\item{url}{Character. The URL field.}
\item{notes}{Character. The notes field.}
}
}
\description{
Reads entries from a KeePass \code{.kdbx} database file (versions 3.x and 4.x).
}
\details{
At least one of \code{password} or \code{keyfile} must be provided.
The function auto-detects the KDBX version (3.x or 4.x).
Protected fields (like passwords) are automatically decrypted.
}
\examples{
\dontrun{
db <- kdbx_read("my_database.kdbx", password = "secret")
db[db$group_path == "Root/Email", ]
}
}
